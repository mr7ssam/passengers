name: p_bootstrap
description: passengers bootstrap.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  easy_localization: ^3.0.1-dev

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
